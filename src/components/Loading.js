import React, {Component} from "react";

class Loading extends Component {
  render() {
    return (
      <p>Criatura, escribe algo o no vamos a ningún sitio :)</p>
    );
  }
}

export default Loading;